#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	//打开文件
	FILE* pf = fopen("test.dat","w");
	if (pf == NULL)
	{
		perror("fopen");
		return 1;
	}
	//写入数据

	//关闭文件
	fclose(pf);
	pf = NULL;
	return 0;


}
